version: 2.1
orbs:
  node: circleci/node@4.1.0
  eb: circleci/aws-elastic-beanstalk@1.0.0
  aws-cli: circleci/aws-cli@2.0

jobs:
  build:
    docker:
      - image: "cimg/base:stable"
    steps:
      - node/install
      - checkout
      # this will go and grab your aws credentials that you have saved in circleci
      - aws-cli/setup 
      - run:
        - name: Front-End Install
        - command: |
            npm run frontend:install
      - run:
        - name: Back-End Install
        - command: |
            npm run backend:install
      - run:
        - name: Front-End Build
        - command: |
            npm run frontend:build
      - run:
        - name: Back-End Build
        - command: |
            npm run backend:build

  deploy:
    docker:
      - image: "cimg/base:stable"
    steps:
      - run:
        - name: Front-End Deploy
        - command: |
            npm run frontend:deploy
      - run:
        - name: Back-End Deploy
        - command: |
            npm run backend:deploy

workflows:
  udagram:
    jobs:
      - build
      - deploy
#   deploy:
#     docker:
#       - image: "cimg/node:11.11"
#     steps:
#       - checkout

#       # download and cash dependencies
#       - restore_cash:
#           keys:
#           # you need to update package.json file path as their is 3 package.json files
#             - v1_dependencies-{{ checksum package.json }}
#             - v1_dependencies-
#       - run: 
#           name: install dependencies
#           command: npm install 
# workflows: 
#   build_deploy:
#     jobs: 
#       - deploy
